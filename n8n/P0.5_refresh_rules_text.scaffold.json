{
 "name": "AD4 P0.5 - Refresh Rules Text",
 "active": false,
 "settings": {
  "executionOrder": "v1"
 },
 "nodes": [
  {
   "id": "warn",
   "name": "Note warn",
   "type": "n8n-nodes-base.stickyNote",
   "typeVersion": 1,
   "position": [
    -560,
    -60
   ],
   "parameters": {
    "content": "## READ BEFORE ACTIVATING\n\nYour n8n ALREADY RUNS a P0.5. This file is a rebuild, not a patch.\n\n**Never leave both Active.** Two workflows writing the same tables is\nworse than one.\n\nSafe cutover:\n  1. Import this. Leave it INACTIVE.\n  2. Fill in Config.\n  3. Run it once with the Manual Trigger (or the AD4 Workflows page).\n  4. Compare its Summary line against your original's.\n  5. Only then: deactivate the original, activate this.",
    "width": 480,
    "height": 340
   }
  },
  {
   "id": "endpoint",
   "name": "Note endpoint",
   "type": "n8n-nodes-base.stickyNote",
   "typeVersion": 1,
   "position": [
    -560,
    320
   ],
   "parameters": {
    "content": "## The Polymarket endpoint is NOT verified\n\n`rules_url` is pre-filled with the Gamma markets endpoint filtered by\ncondition id. It could NOT be tested when this file was built - the build\nsandbox blocks polymarket.com.\n\n`{condition}` is replaced with each open market's `condition_id`, one\nrequest per market.\n\nYour working P0.5 has the real one: open it, click its Polymarket HTTP\nnode, copy the URL across.",
    "width": 480,
    "height": 300
   }
  },
  {
   "id": "why",
   "name": "Note why",
   "type": "n8n-nodes-base.stickyNote",
   "typeVersion": 1,
   "position": [
    1180,
    380
   ],
   "parameters": {
    "content": "## Why rules text is worth a daily job\n\nThe rules say WHICH STATION and WHICH SURFACE settles the market. If\nPolymarket edits that mid-life, every probability AD4 computed against the\nold station is measuring the wrong thing - and nothing else in the system\nwould notice.\n\nThis workflow stamps `rules_changed_at` ONLY when the text actually moves,\nso the column answers \"when did the terms last change\", not \"when did we\nlast poll\".\n\nA run that finds a change reports status `attention`, so it stands out on\nthe AD4 Workflows page and in the P4.1 watchdog.\n\nFind changes:\n\n    select city_key, resolution_date, rules_changed_at\n    from markets\n    where rules_changed_at > now() - interval '7 days'\n    order by rules_changed_at desc;",
    "width": 480,
    "height": 420
   }
  },
  {
   "id": "trigger-manual",
   "name": "Manual Trigger",
   "type": "n8n-nodes-base.manualTrigger",
   "typeVersion": 1,
   "position": [
    0,
    0
   ],
   "parameters": {}
  },
  {
   "id": "trigger-schedule",
   "name": "Schedule Trigger",
   "type": "n8n-nodes-base.scheduleTrigger",
   "typeVersion": 1.2,
   "position": [
    0,
    160
   ],
   "parameters": {
    "rule": {
     "interval": [
      {
       "field": "days",
       "daysInterval": 1
      }
     ]
    }
   }
  },
  {
   "id": "trigger-webhook",
   "name": "Webhook Trigger",
   "type": "n8n-nodes-base.webhook",
   "typeVersion": 2,
   "position": [
    0,
    320
   ],
   "parameters": {
    "httpMethod": "POST",
    "path": "ad4-refresh-rules",
    "responseMode": "responseNode",
    "options": {
     "allowedOrigins": "*"
    }
   },
   "webhookId": "ad4-refresh-rules"
  },
  {
   "id": "config",
   "name": "Config",
   "type": "n8n-nodes-base.set",
   "typeVersion": 3.4,
   "position": [
    240,
    100
   ],
   "parameters": {
    "assignments": {
     "assignments": [
      {
       "id": "1",
       "name": "supabase_url",
       "type": "string",
       "value": ""
      },
      {
       "id": "2",
       "name": "service_key",
       "type": "string",
       "value": ""
      },
      {
       "id": "3",
       "name": "rules_url",
       "type": "string",
       "value": ""
      },
      {
       "id": "4",
       "name": "max_markets_per_run",
       "type": "string",
       "value": "0"
      }
     ]
    }
   }
  },
  {
   "id": "load-markets",
   "name": "Load open markets",
   "type": "n8n-nodes-base.httpRequest",
   "typeVersion": 4.2,
   "position": [
    460,
    100
   ],
   "parameters": {
    "method": "GET",
    "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/markets?select=market_id,city_key,event_slug,condition_id,rules_text,rules_changed_at&closed=eq.false&condition_id=not.is.null&limit=2000",
    "sendHeaders": true,
    "headerParameters": {
     "parameters": [
      {
       "name": "apikey",
       "value": "={{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Authorization",
       "value": "=Bearer {{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Content-Type",
       "value": "application/json"
      },
      {
       "name": "Accept",
       "value": "application/json"
      }
     ]
    }
   },
   "alwaysOutputData": true,
   "onError": "continueRegularOutput"
  },
  {
   "id": "build-requests",
   "name": "Build request list",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    680,
    100
   ],
   "parameters": {
    "jsCode": "// One request per open market.\nconst cfg = $('Config').first().json;\nconst template = String(cfg.rules_url || '');\nif (!template.includes('{condition}')) {\n  throw new Error('rules_url must contain the placeholder {condition}. Got: ' + template);\n}\nconst markets = $('Load open markets').all().map(i => i.json).flat();\nconst cap = Number(cfg.max_markets_per_run || 0) || 0;\n\nconst out = [];\nfor (const m of markets) {\n  if (!m || !m.market_id || !m.condition_id) continue;\n  out.push({ json: {\n    market_id: m.market_id,\n    condition_id: m.condition_id,\n    rules_url: template.replace('{condition}', encodeURIComponent(m.condition_id)),\n  }});\n  if (cap && out.length >= cap) break;\n}\nif (out.length === 0) {\n  throw new Error('No open markets with a condition_id came back from Supabase. ' +\n                  'Nothing to refresh - this is fine if every market is closed.');\n}\nreturn out;"
   }
  },
  {
   "id": "fetch-rules",
   "name": "Fetch rules",
   "type": "n8n-nodes-base.httpRequest",
   "typeVersion": 4.2,
   "position": [
    900,
    100
   ],
   "parameters": {
    "method": "GET",
    "url": "={{ $json.rules_url }}",
    "sendHeaders": true,
    "headerParameters": {
     "parameters": [
      {
       "name": "Accept",
       "value": "application/json"
      }
     ]
    },
    "options": {
     "batching": {
      "batch": {
       "batchSize": 10,
       "batchInterval": 400
      }
     },
     "timeout": 20000
    }
   },
   "alwaysOutputData": true,
   "onError": "continueRegularOutput"
  },
  {
   "id": "diff",
   "name": "Diff rules",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    1120,
    100
   ],
   "parameters": {
    "jsCode": "// Compare each market's fetched rules text against what we already stored.\n//\n// CONFIRMED columns: markets.rules_text, rules_fetched_at, rules_changed_at\n// (sql/ad4_99_verify.sql section 7).\nconst existing = $('Load open markets').all().map(i => i.json).flat();\nconst requests = $('Build request list').all().map(i => i.json);\nconst responses = $input.all().map(i => i.json);\nconst now = new Date().toISOString();\n\nconst prior = new Map(existing.map(m => [String(m.market_id), m]));\nconst rows = [], changed = [];\nlet failed = 0;\n\nfor (let i = 0; i < requests.length; i++) {\n  const req = requests[i];\n  const res = responses[i];\n  if (!res || res.error) { failed++; continue; }\n\n  // the payload may be the market object, an array of them, or wrapped\n  const cand = Array.isArray(res) ? res[0]\n             : (res.data ? (Array.isArray(res.data) ? res.data[0] : res.data) : res);\n  if (!cand) { failed++; continue; }\n\n  const text = String(cand.rules || cand.description || cand.rules_text || '').trim();\n  if (!text) { failed++; continue; }\n\n  const before = prior.get(String(req.market_id));\n  if (!before) continue;\n\n  // A market we have never fetched before has not CHANGED - we are just\n  // seeing it for the first time. Stamping rules_changed_at there would\n  // make every newly discovered market look like an edited one, which is\n  // precisely the signal this workflow exists to provide.\n  const hadText = Boolean(String(before.rules_text || '').trim());\n  const didChange = hadText && String(before.rules_text).trim() !== text;\n  if (didChange) {\n    changed.push({ market_id: req.market_id, city_key: before.city_key ?? null });\n  }\n\n  rows.push({\n    market_id: req.market_id,\n    rules_text: text,\n    rules_fetched_at: now,\n    // Only stamp rules_changed_at when the text actually moved.\n    rules_changed_at: didChange ? now : (before.rules_changed_at || null),\n  });\n}\n\nreturn [{ json: { rows, n: rows.length, changed, n_changed: changed.length,\n                  failed, requested: requests.length } }];"
   }
  },
  {
   "id": "guard",
   "name": "Guard: did anything parse?",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    1340,
    100
   ],
   "parameters": {
    "jsCode": "const p = $('Diff rules').first().json;\nif (p.n === 0) {\n  throw new Error(\n    `Parsed rules for 0 of ${p.requested} markets (${p.failed} failed). ` +\n    `Nothing was written. Most likely the Polymarket rules endpoint in Config ` +\n    `is wrong, or its response carries the text under a different key than ` +\n    `rules / description / rules_text. Open your original P0.5 and copy its ` +\n    `URL across.`);\n}\nreturn [{ json: p }];"
   }
  },
  {
   "id": "upsert-rules",
   "name": "Upsert rules",
   "type": "n8n-nodes-base.httpRequest",
   "typeVersion": 4.2,
   "position": [
    1560,
    100
   ],
   "parameters": {
    "method": "POST",
    "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/markets?on_conflict=market_id",
    "sendHeaders": true,
    "headerParameters": {
     "parameters": [
      {
       "name": "apikey",
       "value": "={{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Authorization",
       "value": "=Bearer {{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Content-Type",
       "value": "application/json"
      },
      {
       "name": "Accept",
       "value": "application/json"
      },
      {
       "name": "Prefer",
       "value": "resolution=merge-duplicates,return=minimal"
      }
     ]
    },
    "sendBody": true,
    "specifyBody": "json",
    "jsonBody": "={{ JSON.stringify($json.rows) }}",
    "options": {
     "response": {
      "response": {
       "neverError": true
      }
     }
    }
   },
   "alwaysOutputData": true,
   "onError": "continueRegularOutput"
  },
  {
   "id": "summary",
   "name": "Summary",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    1780,
    100
   ],
   "parameters": {
    "jsCode": "// DID THE DATABASE ACTUALLY TAKE IT?\n//\n// Every write node here carries onError=continueRegularOutput, so the workflow\n// walks straight past a rejected write and reaches this node looking healthy.\n// That is how a run reports success having written nothing - which is exactly\n// what happened: reads worked, every write was refused, and the only visible\n// error was on the LAST node in the chain.\n//\n// With neverError on, a refusal arrives as the PostgREST error body instead of\n// an exception, so it can be read and named here.\nfunction writeError(node) {\n  let items;\n  try { items = $(node).all(); } catch (e) { return null; }\n  for (const it of items || []) {\n    const b = it && it.json;\n    if (!b || typeof b !== 'object') continue;\n    if (b.error && !b.code) return { node, code: null, message: String(b.error).slice(0, 300) };\n    if (b.code || b.message) {\n      return { node, code: b.code ?? null,\n               message: b.message ?? '', details: b.details ?? null, hint: b.hint ?? null };\n    }\n  }\n  return null;\n}\n\nconst WRITE_NODES = [\"Upsert rules\"];\nfor (const node of WRITE_NODES) {\n  const e = writeError(node);\n  if (!e) continue;\n  // 42501 is the one worth naming, because the cause is never the code.\n  if (String(e.code) === '42501' || /permission denied/i.test(e.message)) {\n    throw new Error(\n      `Supabase REFUSED the write: ${e.message} (${e.code}). This is the ANON key in the ` +\n      `Config node, not the service key - anon is granted SELECT and nothing else, which is ` +\n      `why reads worked and every write was silently dropped. Open Config -> service_key and ` +\n      `paste the SERVICE ROLE key (Supabase -> Project Settings -> API -> service_role, or a ` +\n      `key beginning sb_secret_). NEVER put that key anywhere starting NEXT_PUBLIC_. ` +\n      `Failing node: ${e.node}. Nothing was written.`);\n  }\n  throw new Error(\n    `Supabase refused the write at \"${e.node}\": ${e.message}` +\n    (e.code ? ` (${e.code})` : '') + (e.details ? ` - ${e.details}` : '') +\n    (e.hint ? ` Hint: ${e.hint}` : '') + ' Nothing was written.');\n}\n\nconst p = $('Diff rules').first().json;\nconst rows = p.n || 0;\nconst changed = p.n_changed || 0;\nconst list = changed > 0 ? (p.changed || []).map(c => c.city_key || c.market_id).join(', ') : '';\nconst summary = changed > 0\n  ? `AD4 P0.5: ${rows} markets refreshed. RULES CHANGED on ${changed}: ${list}. ` +\n    `Re-read those before trusting any probability computed against them.`\n  : `AD4 P0.5: ${rows} markets refreshed, no rule changes.`;\n// A rules change is not a failure, but it invalidates model output computed\n// against the old text - 'attention' so the UI and the watchdog both see it.\nconst status = changed > 0 ? 'attention' : 'ok';\nconst detail = { n_changed: changed, changed: p.changed ?? [],\n                 failed: p.failed, requested: p.requested };\nreturn [{ json: {\n  summary, status, rows,\n  log: { p_job: 'P0.5_refresh_rules_text', p_status: status, p_rows: rows,\n         p_detail: { ...detail, summary, trigger: $execution.mode, execution_id: $execution.id } },\n} }];"
   }
  },
  {
   "id": "log-run",
   "name": "Log run",
   "type": "n8n-nodes-base.httpRequest",
   "typeVersion": 4.2,
   "position": [
    2000,
    100
   ],
   "parameters": {
    "method": "POST",
    "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/rpc/log_ingest",
    "sendHeaders": true,
    "headerParameters": {
     "parameters": [
      {
       "name": "apikey",
       "value": "={{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Authorization",
       "value": "=Bearer {{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Content-Type",
       "value": "application/json"
      },
      {
       "name": "Accept",
       "value": "application/json"
      }
     ]
    },
    "sendBody": true,
    "specifyBody": "json",
    "jsonBody": "={{ JSON.stringify($json.log) }}"
   },
   "alwaysOutputData": true,
   "onError": "continueRegularOutput"
  },
  {
   "id": "respond",
   "name": "Respond",
   "type": "n8n-nodes-base.respondToWebhook",
   "typeVersion": 1,
   "position": [
    2220,
    100
   ],
   "parameters": {
    "respondWith": "json",
    "responseBody": "={{ JSON.stringify($('Summary').first().json) }}"
   }
  },
  {
   "id": "sched-check",
   "name": "Check schedule",
   "type": "n8n-nodes-base.httpRequest",
   "typeVersion": 4.2,
   "position": [
    430,
    -30
   ],
   "alwaysOutputData": true,
   "onError": "continueRegularOutput",
   "parameters": {
    "method": "POST",
    "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/rpc/should_run",
    "sendHeaders": true,
    "headerParameters": {
     "parameters": [
      {
       "name": "apikey",
       "value": "={{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Authorization",
       "value": "=Bearer {{ $('Config').first().json.service_key }}"
      },
      {
       "name": "Content-Type",
       "value": "application/json"
      },
      {
       "name": "Accept",
       "value": "application/json"
      }
     ]
    },
    "sendBody": true,
    "specifyBody": "json",
    "jsonBody": "={{ JSON.stringify({ p_job: \"P0.5_refresh_rules_text\", p_trigger: $execution.mode === \"trigger\" ? \"schedule\" : $execution.mode }) }}",
    "options": {
     "timeout": 10000,
     "response": {
      "response": {
       "neverError": true
      }
     }
    }
   }
  },
  {
   "id": "sched-gate",
   "name": "Run now?",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    620,
    -30
   ],
   "parameters": {
    "jsCode": "\n// AN ENDPOINT THIS FILE CANNOT KNOW.\n//\n// P0.2-P0.5 are RECONSTRUCTIONS. The Supabase half is grounded in the real\n// schema and is safe; the Polymarket half never was. These files used to ship\n// with a plausible-looking URL already filled in, which is worse than shipping\n// none: it reads as authoritative, so nobody changes it, and the run dies four\n// nodes later with a message about response shapes. That is exactly how this\n// wasted an afternoon.\n//\n// So the field ships EMPTY and the run stops here - before any HTTP call,\n// before any database read - saying where the real value is.\n{\n  const cfg = $('Config').first().json ?? {};\n  const missing = Object.keys(cfg)\n    .filter((k) => k.endsWith('_url') && k !== 'supabase_url')\n    .filter((k) => !String(cfg[k] ?? '').trim());\n  if (missing.length) {\n    throw new Error(\n      `Config.${missing.join(' and Config.')} is empty, and this file cannot fill it in. ` +\n      `Open your OWN working P0.2-P0.5 in n8n, click its Polymarket HTTP node, copy the URL ` +\n      `from it, and paste it here. Those workflows are what put 841 markets and 122,000 trades ` +\n      `in this database - they are the authority on the endpoint, not this reconstruction. ` +\n      `Nothing was fetched and nothing was written.`);\n  }\n}\n\n// Ask the database whether this run should do anything.\n//\n// n8n cannot be told from outside not to FIRE a schedule trigger, so the saving\n// is here: returning no items stops every node below, and the execution ends\n// having done two trivial requests instead of the whole job.\n//\n// settings.workflow_schedules owns the answer (sql/ad4_20_schedules.sql) and\n// the Workflows page edits it. A manual or webhook run always proceeds - a Run\n// button that obeyed the cadence would be a Run button that does nothing.\nconst d = $('Check schedule').first().json ?? {};\nconst decision = Array.isArray(d) ? d[0] : d;\n\n// A PERMISSION ERROR IS NOT AN UNAVAILABLE GATE. 42501 here means the key\n// cannot call should_run, and a key that cannot do that cannot write either -\n// so stop, rather than failing open into a run that writes nothing.\n//\n// THIS IS NOT THE MAIN DEFENCE AND MUST NOT BE MISTAKEN FOR IT. sql/ad4_20\n// grants should_run to anon ON PURPOSE, so the Workflows page can preview a\n// schedule change before saving it. On a database with that grant - which is\n// every database that ran ad4_20 - an anon key sails straight through this\n// check and only fails at the first WRITE. The write guard in the Summary\n// node is what actually catches it; run section 6 of sql/ad4_diagnose.sql to\n// see which key can write what before spending a run finding out.\nif (decision && (String(decision.code) === '42501' ||\n                 /permission denied/i.test(String(decision.message || '')))) {\n  throw new Error(\n    `Supabase refused should_run: ${decision.message} (${decision.code}). This is the ANON ` +\n    `key in the Config node, not the service key - anon is granted SELECT and nothing else. ` +\n    `Every write in this workflow would be refused the same way. Open Config -> service_key ` +\n    `and paste the SERVICE ROLE key (Supabase -> Project Settings -> API -> service_role, or ` +\n    `a key beginning sb_secret_). NEVER put that key anywhere starting NEXT_PUBLIC_.`);\n}\n\n// A gate that fails closed would silently disable every workflow the first\n// time the settings row is missing or the RPC is absent. Fail OPEN: an\n// unreachable gate means run, and say so in the log.\nif (decision === null || decision === undefined || decision.run === undefined) {\n  console.log('schedule gate unavailable - running anyway');\n  return $input.all();\n}\n\nif (decision.run === false) {\n  // Recorded, so a skipped run is visible on the Workflows page as a skip\n  // rather than looking like a workflow that silently stopped working.\n  return [{ json: { __skipped: true, reason: decision.reason || 'not scheduled',\n                    minutes_since: decision.minutes_since ?? null,\n                    every_minutes: decision.every_minutes ?? null } }];\n}\nreturn $input.all();"
   }
  },
  {
   "id": "sched-stop",
   "name": "Stop if skipped",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    620,
    -30
   ],
   "parameters": {
    "jsCode": "// Returning [] stops the workflow here. Everything below this node\n// is skipped, which is the entire point of the gate.\nconst items = $input.all();\nif (items.length === 1 && items[0].json && items[0].json.__skipped) {\n  console.log('skipped: ' + items[0].json.reason);\n  return [];\n}\nreturn items;"
   }
  }
 ],
 "connections": {
  "Manual Trigger": {
   "main": [
    [
     {
      "node": "Config",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Schedule Trigger": {
   "main": [
    [
     {
      "node": "Config",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Webhook Trigger": {
   "main": [
    [
     {
      "node": "Config",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Config": {
   "main": [
    [
     {
      "node": "Check schedule",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Load open markets": {
   "main": [
    [
     {
      "node": "Build request list",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Build request list": {
   "main": [
    [
     {
      "node": "Fetch rules",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Fetch rules": {
   "main": [
    [
     {
      "node": "Diff rules",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Diff rules": {
   "main": [
    [
     {
      "node": "Guard: did anything parse?",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Guard: did anything parse?": {
   "main": [
    [
     {
      "node": "Upsert rules",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Upsert rules": {
   "main": [
    [
     {
      "node": "Summary",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Summary": {
   "main": [
    [
     {
      "node": "Log run",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Log run": {
   "main": [
    [
     {
      "node": "Respond",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Check schedule": {
   "main": [
    [
     {
      "node": "Run now?",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Run now?": {
   "main": [
    [
     {
      "node": "Stop if skipped",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Stop if skipped": {
   "main": [
    [
     {
      "node": "Load open markets",
      "type": "main",
      "index": 0
     }
    ]
   ]
  }
 }
}
