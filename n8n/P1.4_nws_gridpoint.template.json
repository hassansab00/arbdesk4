{
  "name": "AD4 P1.4 - NWS Gridpoint",
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveDataSuccessExecution": "none",
    "saveDataErrorExecution": "all",
    "saveExecutionProgress": false,
    "saveManualExecutions": false,
    "executionTimeout": 900
  },
  "nodes": [
    {
      "id": "why",
      "name": "Note why",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -560,
        -60
      ],
      "parameters": {
        "content": "## What this is for\n\nad4_21 established that a day's maximum is largely settled by conditions\nobservable in the morning: dewpoint depression, cloud, rain. But those come\nfrom `weather_observations`, so the desk could only EXPLAIN a day after its\nmorning had happened. It could not use them to predict tomorrow.\n\napi.weather.gov already forecasts every one of them. This reads the RAW\ngridpoint - `/gridpoints/{wfo}/{x},{y}`, not the `/forecast` convenience\nendpoint P1.3 uses - which returns about forty time series. AD4 was taking\nthe temperature out of it and throwing the rest away.\n\nThe columns written here are named to MATCH v_city_day_features exactly, so\nthe model fitted on observed conditions applies to forecast ones with no\ntranslation layer. That is what turns \"explain today\" into \"predict\ntomorrow\".",
        "width": 480,
        "height": 420
      }
    },
    {
      "id": "traps",
      "name": "Note traps",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -560,
        400
      ],
      "parameters": {
        "content": "## Three unit traps, all handled\n\nThe raw gridpoint does NOT speak in the units the observations table uses,\nand every one of these would be wrong in a way that still looks plausible:\n\n  skyCover      NWS gives PERCENT (0-100). weather_observations.cloud_cover\n                is OKTAS (0-8), because that is what METAR reports. Storing\n                a percent in an oktas column would make an overcast day read\n                as 100 oktas and every cloud coefficient meaningless.\n\n  quantitativePrecipitation   millimetres. The observed column is INCHES.\n\n  windSpeed     km/h. The observed column is KNOTS.\n\nAnd `validTime` is an ISO interval, not an instant:\n`2026-09-04T12:00:00+00:00/PT6H` means this one value covers SIX HOURS. A\nparser that treated it as a single hour would weight a six-hour block the\nsame as a one-hour block and skew every daily average.",
        "width": 480,
        "height": 460
      }
    },
    {
      "id": "trigger-manual",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "parameters": {}
    },
    {
      "id": "trigger-schedule",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        160
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "hours",
              "hoursInterval": 6
            }
          ]
        }
      }
    },
    {
      "id": "trigger-webhook",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        320
      ],
      "parameters": {
        "httpMethod": "POST",
        "path": "ad4-nws-gridpoint",
        "responseMode": "responseNode",
        "options": {
          "allowedOrigins": "*"
        }
      },
      "webhookId": "ad4-nws-gridpoint"
    },
    {
      "id": "config",
      "name": "Config",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        240,
        100
      ],
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "1",
              "name": "supabase_url",
              "type": "string",
              "value": ""
            },
            {
              "id": "3",
              "name": "user_agent",
              "type": "string",
              "value": "ArbDesk4 (github.com/hassansab00/arbdesk4)"
            },
            {
              "id": "4",
              "name": "max_horizon_days",
              "type": "string",
              "value": "7"
            },
            {
              "id": "5",
              "name": "peak_window",
              "type": "string",
              "value": "12-18"
            },
            {
              "id": "6",
              "name": "morning_hour",
              "type": "string",
              "value": "8"
            },
            {
              "id": "7",
              "name": "max_cities_per_run",
              "type": "string",
              "value": "0"
            }
          ]
        }
      }
    },
    {
      "id": "load-cities",
      "name": "Load cities",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        460,
        100
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/cities?select=city_key,timezone,nws_supported,nws_grid_wfo,nws_grid_x,nws_grid_y&nws_grid_wfo=not.is.null&limit=200",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi"
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "credentials": {
        "supabaseApi": {
          "id": "ad4-supabase",
          "name": "AD4 Supabase"
        }
      }
    },
    {
      "id": "build-requests",
      "name": "Build requests",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        680,
        100
      ],
      "parameters": {
        "jsCode": "// One item per city that has a cached NWS gridpoint. P1.2 and P1.3 write\n// nws_grid_wfo/x/y onto `cities`; this needs no /points lookup of its own.\nconst cfg = $('Config').first().json;\nconst cities = $('Load cities').all().map(i => i.json).flat();\nconst cap = Number(cfg.max_cities_per_run || 0) || 0;\n\nconst out = [];\n// SCOPE. run_scope() decides which cities this run covers and should_run()\n// carries the answer, so this costs no extra node and no extra request. A\n// workflow that ignores the field behaves exactly as it always did; absent,\n// \"all\", or a selection matching nothing all mean every active city, because\n// a scope that silently reduces a run to zero is indistinguishable from a\n// broken job.\nconst _scope = (($('Check schedule').first().json ?? {}).scope) ?? {};\nconst _only = Array.isArray(_scope.cities) && _scope.cities.length\n  ? new Set(_scope.cities) : null;\n\nfor (const c of cities) {\n  if (!c || !c.city_key) continue;\n  if (_only && !_only.has(c.city_key)) continue;\n  if (c.nws_supported === false) continue;\n  if (!c.nws_grid_wfo || c.nws_grid_x == null || c.nws_grid_y == null) continue;\n  out.push({ json: {\n    city_key: c.city_key,\n    timezone: c.timezone || 'UTC',\n    grid: `${c.nws_grid_wfo}/${c.nws_grid_x},${c.nws_grid_y}`,\n    url: `https://api.weather.gov/gridpoints/${c.nws_grid_wfo}/${c.nws_grid_x},${c.nws_grid_y}`,\n  }});\n  if (cap && out.length >= cap) break;\n}\nif (out.length === 0) {\n  throw new Error(\n    'No city has a cached NWS gridpoint. P1.2 or P1.3 must run first - they ' +\n    'resolve /points and store nws_grid_wfo/x/y on `cities`.');\n}\nreturn out;"
      }
    },
    {
      "id": "fetch-grid",
      "name": "Fetch gridpoint",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        900,
        100
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "User-Agent",
              "value": "={{ $('Config').first().json.user_agent }}"
            },
            {
              "name": "Accept",
              "value": "application/geo+json"
            }
          ]
        },
        "options": {
          "batching": {
            "batch": {
              "batchSize": 10,
              "batchInterval": 200
            }
          },
          "timeout": 20000,
          "response": {
            "response": {
              "neverError": true,
              "responseFormat": "json"
            }
          }
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput"
    },
    {
      "id": "build-rows",
      "name": "Build rows",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        100
      ],
      "parameters": {
        "jsCode": "// Turn ~40 forecast series into one row per city-day, in the units the\n// observations table already uses.\nconst cfg = $('Config').first().json;\nconst horizon = Number(cfg.max_horizon_days || 7);\nconst [winFrom, winTo] = String(cfg.peak_window || '12-18').split('-').map(Number);\nconst morningHour = Number(cfg.morning_hour || 8);\n\nconst reqs = $('Build requests').all().map(i => i.json);\n// THE BUG THAT MADE ALL 37 CITIES \"FAIL\".\n// n8n's HTTP node autodetects the response format from Content-Type, and its\n// test is `contentType.includes('application/json')`. api.weather.gov answers\n// `application/geo+json` (and `application/problem+json` on an error), neither\n// of which contains that substring - so every response was decoded as TEXT and\n// arrived as `{ data: \"{\\\"properties\\\":...\" }`. No `properties`, no `status`,\n// no `title`: not a 404, not an error, just nothing to read. Hence \"0 are not\n// US, 37 failed\" with no detail to report, in both P1.2 and P1.3, forever.\n// The Fetch nodes now set responseFormat=json explicitly. This re-parses a\n// string body regardless, so the workflow also works on an n8n build that\n// names that option differently.\nfunction asJson(v) {\n  if (v == null) return {};\n  if (typeof v === 'string') {\n    const s = v.trim();\n    if (s.startsWith('{') || s.startsWith('[')) { try { return JSON.parse(s); } catch (e) {} }\n    return { error: 'unparseable body: ' + s.slice(0, 200) };\n  }\n  if (typeof v !== 'object') return {};\n  if (typeof v.data === 'string') {\n    const s = v.data.trim();\n    if (s.startsWith('{') || s.startsWith('[')) { try { return JSON.parse(s); } catch (e) {} }\n  }\n  if (typeof v.body === 'string') {\n    const s = v.body.trim();\n    if (s.startsWith('{') || s.startsWith('[')) { try { return JSON.parse(s); } catch (e) {} }\n  }\n  return v;\n}\n\nconst res  = $('Fetch gridpoint').all().map(i => asJson(i.json));\n\nif (res.length !== reqs.length) {\n  throw new Error(\n    `Gridpoint fetch returned ${res.length} responses for ${reqs.length} cities. ` +\n    `Rows pair by position, so a mismatch would file one city's forecast under ` +\n    `another. Nothing written.`);\n}\n\n// A validTime is an ISO INTERVAL - \"2026-09-04T12:00:00+00:00/PT6H\" - so one\n// value can cover six hours. Expanding it is not optional: treating it as a\n// single hour weights a six-hour block the same as a one-hour block and skews\n// every daily average it lands in.\nfunction expand(validTime) {\n  const [startStr, dur] = String(validTime || '').split('/');\n  const start = Date.parse(startStr);\n  if (!Number.isFinite(start)) return [];\n  // PnDTnH: days and hours are the only parts NWS uses here.\n  const m = /^P(?:(\\d+)D)?(?:T(?:(\\d+)H)?(?:(\\d+)M)?)?$/.exec(dur || 'PT1H');\n  const hours = m ? (Number(m[1] || 0) * 24 + Number(m[2] || 0) + (Number(m[3] || 0) >= 30 ? 1 : 0)) : 1;\n  const out = [];\n  for (let h = 0; h < Math.max(1, hours); h++) out.push(start + h * 3600_000);\n  return out;\n}\n\n// ACCUMULATING vs INSTANTANEOUS series - the subtlety that makes expanding an\n// interval correct for one and wrong for the other.\n//\n// Temperature over a six-hour block means \"it is 22C for each of these six\n// hours\", so every expanded hour carries 22 and averaging works.\n// Precipitation over a six-hour block means \"25mm falls across these six\n// hours IN TOTAL\". Give every expanded hour 25mm and summing the day returns\n// 150mm - which is exactly the bug this comment exists because of: the first\n// version reported 6 inches of rain where the forecast said one.\n//\n// So an accumulating series is divided by the hours it spans, and summing the\n// expanded points recovers the block total.\nconst ACCUMULATING = new Set(['quantitativePrecipitation', 'snowfallAmount', 'iceAccumulation']);\n\n// The local calendar day and hour for an instant, in the city's own zone -\n// which is the day a daily maximum belongs to.\nfunction localParts(ms, tz) {\n  try {\n    const p = new Intl.DateTimeFormat('en-CA', {\n      timeZone: tz, year: 'numeric', month: '2-digit', day: '2-digit',\n      hour: '2-digit', hour12: false,\n    }).formatToParts(new Date(ms));\n    const g = (t) => p.find(x => x.type === t)?.value;\n    return { date: `${g('year')}-${g('month')}-${g('day')}`, hour: Number(g('hour')) };\n  } catch {\n    const d = new Date(ms);\n    return { date: d.toISOString().slice(0, 10), hour: d.getUTCHours() };\n  }\n}\n\n// ---- unit conversions, each one a trap that still looks plausible if wrong -\nconst pctToOktas = (v) => v === null ? null : (v / 100) * 8;   // NWS %, we store oktas\nconst mmToInches = (v) => v === null ? null : v / 25.4;        // NWS mm, we store inches\nconst kmhToKnots = (v) => v === null ? null : v / 1.852;       // NWS km/h, we store knots\n\nfunction series(props, name) {\n  const s = props?.[name];\n  if (!s || !Array.isArray(s.values)) return [];\n  const uom = String(s.uom || '');\n  const accumulates = ACCUMULATING.has(name);\n  return s.values.flatMap(v => {\n    if (v?.value === null || v?.value === undefined) return [];\n    let val = Number(v.value);\n    if (!Number.isFinite(val)) return [];\n    // The gridpoint states its own unit. Honour it rather than assume degC:\n    // a server answering degF into a Celsius column is the same class of bug\n    // as the three conversions above.\n    if (name.toLowerCase().includes('temperature') || name === 'dewpoint') {\n      if (uom.includes('degF')) val = (val - 32) * 5 / 9;\n    }\n    const hours = expand(v.validTime);\n    if (!hours.length) return [];\n    // Spread an accumulating total across the hours it covers, so a sum over\n    // the day recovers the forecast rather than multiplying it by the block\n    // length.\n    const per = accumulates ? val / hours.length : val;\n    return hours.map(ms => ({ ms, val: per }));\n  });\n}\n\nconst rows = [];\nlet ok = 0, failed = 0, partial = 0;\nconst runAtFallback = new Date().toISOString();\n\nfor (let i = 0; i < reqs.length; i++) {\n  const req = reqs[i], r = res[i] || {};\n  const props = r.properties || {};\n  if (!props.temperature) { failed++; continue; }\n\n  if (props.gridId && !req.grid.startsWith(props.gridId)) {\n    throw new Error(\n      `Gridpoint ${props.gridId} came back where ${req.grid} (${req.city_key}) was ` +\n      `expected. Responses are out of order - nothing written.`);\n  }\n\n  const runAt = props.updateTime || runAtFallback;\n  const tz = req.timezone;\n\n  // Bucket every series by local day.\n  const byDay = new Map();\n  const put = (key, ms, val) => {\n    const { date, hour } = localParts(ms, tz);\n    if (!byDay.has(date)) byDay.set(date, { hours: new Set(), vals: {} });\n    const d = byDay.get(date);\n    d.hours.add(hour);\n    (d.vals[key] = d.vals[key] || []).push({ hour, val });\n  };\n\n  for (const p of series(props, 'temperature'))                 put('temp', p.ms, p.val);\n  for (const p of series(props, 'dewpoint'))                    put('dew', p.ms, p.val);\n  for (const p of series(props, 'relativeHumidity'))            put('rh', p.ms, p.val);\n  for (const p of series(props, 'skyCover'))                    put('sky', p.ms, p.val);\n  for (const p of series(props, 'windSpeed'))                   put('wind', p.ms, p.val);\n  for (const p of series(props, 'probabilityOfPrecipitation'))  put('pop', p.ms, p.val);\n  for (const p of series(props, 'quantitativePrecipitation'))   put('qpf', p.ms, p.val);\n  for (const p of series(props, 'apparentTemperature'))         put('app', p.ms, p.val);\n\n  const days = [...byDay.keys()].sort();\n  if (!days.length) { failed++; continue; }\n  const today = days[0];\n  let wrote = 0;\n\n  for (const date of days) {\n    const d = byDay.get(date);\n    // The same peak-window rule P1.3 uses: a day missing its afternoon has an\n    // understated maximum, and a low max invents disagreement out of nothing.\n    let covered = true;\n    for (let h = winFrom; h <= winTo; h++) if (!d.hours.has(h)) { covered = false; break; }\n    if (!covered) { partial++; continue; }\n\n    const lead = Math.round((Date.parse(date + 'T00:00:00Z') - Date.parse(today + 'T00:00:00Z')) / 86400000);\n    if (lead < 0 || lead > horizon) continue;\n\n    const at = (k, hour) => {\n      const list = d.vals[k] || [];\n      if (!list.length) return null;\n      let best = list[0];\n      for (const v of list) if (Math.abs(v.hour - hour) < Math.abs(best.hour - hour)) best = v;\n      return best.val;\n    };\n    const agg = (k, fn, filter) => {\n      let list = d.vals[k] || [];\n      if (filter) list = list.filter(v => filter(v.hour));\n      if (!list.length) return null;\n      const vals = list.map(v => v.val);\n      return fn === 'max' ? Math.max(...vals)\n           : fn === 'min' ? Math.min(...vals)\n           : fn === 'sum' ? vals.reduce((s, v) => s + v, 0)\n           : vals.reduce((s, v) => s + v, 0) / vals.length;\n    };\n    const daytime = (h) => h >= 9 && h <= 17;\n\n    const mTemp = at('temp', morningHour);\n    const mDew = at('dew', morningHour);\n    const round = (v, n) => v === null || v === undefined ? null : Number(v.toFixed(n));\n\n    rows.push({\n      city_key: req.city_key, for_date: date, run_at: runAt,\n      source: 'api.weather.gov', lead_days: lead,\n      forecast_max_c: round(agg('temp', 'max'), 2),\n      forecast_min_c: round(agg('temp', 'min'), 2),\n      apparent_max_c: round(agg('app', 'max'), 2),\n      morning_temp_c: round(mTemp, 2),\n      morning_dewpoint_c: round(mDew, 2),\n      dewpoint_depression_c: (mTemp !== null && mDew !== null) ? round(mTemp - mDew, 2) : null,\n      morning_humidity: round(at('rh', morningHour), 1),\n      cloud_mean: round(pctToOktas(agg('sky', 'avg', daytime)), 2),\n      cloud_max: round(pctToOktas(agg('sky', 'max', daytime)), 2),\n      wind_mean: round(kmhToKnots(agg('wind', 'avg', daytime)), 2),\n      wind_max: round(kmhToKnots(agg('wind', 'max', daytime)), 2),\n      precip_total: round(mmToInches(agg('qpf', 'sum')), 4),\n      precip_probability: round(agg('pop', 'max'), 0),\n      n_hours: d.hours.size,\n    });\n    wrote++;\n  }\n  if (wrote) ok++; else failed++;\n}\n\nreturn [{ json: { rows, n_rows: rows.length, cities_ok: ok, cities_failed: failed,\n                  partial_days: partial, requested: reqs.length } }];"
      }
    },
    {
      "id": "guard",
      "name": "Guard: did anything parse?",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1300,
        100
      ],
      "parameters": {
        "jsCode": "const p = $('Build rows').first().json;\nif (p.n_rows === 0) {\n  throw new Error(\n    `Every gridpoint response was unreadable or empty - 0 forecast days from ` +\n    `${p.requested} cities, nothing written. Two things cause this: ` +\n    `\"Fetch gridpoint\" not set to Response Format = JSON (api.weather.gov sends ` +\n    `application/geo+json, which n8n's autodetect decodes as text), or ` +\n    `cities.nws_grid_wfo/x/y not populated yet - P1.2 or P1.3 fills those. ` +\n    `[${p.cities_failed}/${p.requested} failed]`);\n}\nreturn [{ json: p }];\n"
      }
    },
    {
      "id": "write",
      "name": "Write forecast features",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1500,
        100
      ],
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/weather_forecast_features?on_conflict=city_key,for_date,run_at",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Accept",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "resolution=merge-duplicates,return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.rows) }}",
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          }
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi"
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "credentials": {
        "supabaseApi": {
          "id": "ad4-supabase",
          "name": "AD4 Supabase"
        }
      }
    },
    {
      "id": "summary",
      "name": "Summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1700,
        100
      ],
      "parameters": {
        "jsCode": "const AD4_WRITE_NODES = [\"Write forecast features\"];\n// AD4-WRITE-GUARD\n// DID THE DATABASE ACTUALLY TAKE IT?\n//\n// Every write node in this workflow carries onError=continueRegularOutput so\n// that one bad row cannot kill a run. The cost of that is a REFUSED write\n// walks straight past it and arrives here looking healthy - which is how a\n// workflow reports \"ok, 0 rows\" for days while the data quietly stops moving.\n//\n// With neverError on, a refusal arrives as the PostgREST error body instead of\n// an exception, so it can be read and named. That is what this does.\nfunction ad4WriteError(node) {\n  let items;\n  try { items = $(node).all(); } catch (e) { return null; }\n  for (const it of items || []) {\n    const b = it && it.json;\n    if (!b || typeof b !== 'object') continue;\n    if (b.error && !b.code) return { node, code: null, message: String(b.error).slice(0, 300) };\n    if (b.code || b.message) {\n      return { node, code: b.code ?? null, message: b.message ?? '',\n               details: b.details ?? null, hint: b.hint ?? null };\n    }\n  }\n  return null;\n}\n\nfor (const node of AD4_WRITE_NODES) {\n  const e = ad4WriteError(node);\n  if (!e) continue;\n  // 42501 is the one worth naming, because the cause is never in the code.\n  if (String(e.code) === '42501' || /permission denied/i.test(e.message)) {\n    throw new Error(\n      `Supabase REFUSED the write at \"${e.node}\": ${e.message} (${e.code}). The key was ` +\n      `already checked at \"Run now?\" and claims to be the service key, so this is the ` +\n      `DATABASE side: service_role has no INSERT on that table. Run sql/ad4_38_grants.sql ` +\n      `in the Supabase SQL editor - it grants the write path back, then prints one row per ` +\n      `ingest table saying whether the service key can write it. Nothing was written.`);\n  }\n  if (String(e.code) === '42P01') {\n    throw new Error(\n      `Supabase has no such table for \"${e.node}\": ${e.message}. The sql/ files that create ` +\n      `it have not been run - sql/ad4_98_ui_health.sql names which file owns which table. ` +\n      `Nothing was written.`);\n  }\n  throw new Error(\n    `Supabase refused the write at \"${e.node}\": ${e.message}` +\n    (e.code ? ` (${e.code})` : '') + (e.details ? ` - ${e.details}` : '') +\n    (e.hint ? ` Hint: ${e.hint}` : '') + ' Nothing was written.');\n}\n\nconst p = $('Build rows').first().json;\nconst rows = p.n_rows || 0;\nconst summary =\n  `AD4 P1.4: ${rows} forecast-condition day(s) for ${p.cities_ok} cities` +\n  (p.cities_failed ? `, ${p.cities_failed} failed` : '') +\n  (p.partial_days ? `, ${p.partial_days} partial day(s) skipped` : '') + '.';\nconst status = rows > 0 ? (p.cities_failed ? 'attention' : 'ok') : 'attention';\nconst detail = { cities_ok: p.cities_ok, cities_failed: p.cities_failed,\n                 partial_days: p.partial_days, requested: p.requested };\nreturn [{ json: {\n  summary, status, rows,\n  log: { p_job: 'P1.4_nws_gridpoint', p_status: status, p_rows: rows,\n         p_detail: { ...detail, summary, trigger: $execution.mode, execution_id: $execution.id } },\n} }];"
      }
    },
    {
      "id": "log-run",
      "name": "Log run",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1900,
        100
      ],
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/rpc/log_ingest",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.log) }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi"
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "credentials": {
        "supabaseApi": {
          "id": "ad4-supabase",
          "name": "AD4 Supabase"
        }
      }
    },
    {
      "id": "respond",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        2100,
        100
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($('Summary').first().json) }}"
      }
    },
    {
      "id": "sched-check",
      "name": "Check schedule",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        430,
        -30
      ],
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.supabase_url }}/rest/v1/rpc/should_run",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ p_job: \"P1.4_nws_gridpoint\", p_trigger: $execution.mode === \"trigger\" ? \"schedule\" : $execution.mode }) }}",
        "options": {
          "timeout": 10000,
          "response": {
            "response": {
              "neverError": true
            }
          }
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi"
      },
      "credentials": {
        "supabaseApi": {
          "id": "ad4-supabase",
          "name": "AD4 Supabase"
        }
      }
    },
    {
      "id": "sched-gate",
      "name": "Run now?",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        620,
        -30
      ],
      "parameters": {
        "jsCode": "// AD4-KEY-CHECK\n// THE KEY IS NOT IN THIS FILE ANY MORE, AND THAT IS THE POINT.\n//\n// Every Supabase node in this workflow authenticates with an n8n SUPABASE\n// CREDENTIAL rather than a key pasted into the Config node. That is one place\n// to set it instead of twelve, it is encrypted at rest in n8n, and exporting\n// this workflow can no longer leak it - the old shape had the service key\n// sitting in plain text inside a JSON file that gets emailed around.\n//\n// SET IT UP ONCE, BEFORE IMPORTING ANYTHING:\n//   n8n -> Credentials -> New -> Supabase API\n//     Name         AD4 Supabase        <- this name exactly\n//     Host         https://<your-project-ref>.supabase.co\n//     Service Role the SERVICE ROLE key (Project Settings -> API keys)\n//\n// The name matters because every Supabase node in every AD4 workflow is\n// already bound to a credential called AD4 Supabase. Create it first and\n// n8n links all of them on import - nothing to pick node by node. Import\n// first and the nodes come in unbound, and you pick the credential on each\n// one by hand; it works, it is just ~57 dropdowns instead of zero.\n//\n// The credential sends `apikey` and `Authorization: Bearer` itself, which is\n// exactly what these nodes used to set by hand.\n//\n// WHAT STILL CHECKS THE KEY. This node can no longer read it - a credential\n// is not visible to an expression, by design. Two things downstream do:\n//\n//   the schedule gate, immediately below. sql/ad4_38 revokes should_run from\n//   anon, so a credential holding the publishable key is refused HERE, two\n//   nodes in, before a single row is fetched.\n//\n//   the write guard in the Summary node, which names any refusal the\n//   database gives and refuses to report success on a run that wrote nothing.\n//\n// supabase_url stays in Config because an expression has to build the request\n// URL and cannot read the credential's host. It is not a secret.\n{\n  const cfg = $('Config').first().json ?? {};\n  const url = String(cfg.supabase_url ?? '').trim();\n  if (!url) {\n    throw new Error(\n      'Config -> supabase_url is empty. Set it to exactly ' +\n      'https://<your-project-ref>.supabase.co - no trailing slash and no /rest/v1, ' +\n      'which every node below appends itself. The KEY does not go here: it belongs in ' +\n      'the n8n Supabase credential (Credentials -> New -> Supabase API), which is ' +\n      'where the nodes now read it from.');\n  }\n  if (!/^https:\\/\\//.test(url) || /\\/rest\\b/.test(url) || /\\/$/.test(url)) {\n    throw new Error(\n      `Config -> supabase_url is \"${url}\", which is not the shape every node below ` +\n      'expects: https://<your-project-ref>.supabase.co, no trailing slash, no /rest/v1. ' +\n      'Nothing was fetched and nothing was written.');\n  }\n}\n\n// Ask the database whether this run should do anything.\n//\n// n8n cannot be told from outside not to FIRE a schedule trigger, so the saving\n// is here: returning no items stops every node below, and the execution ends\n// having done two trivial requests instead of the whole job.\n//\n// settings.workflow_schedules owns the answer (sql/ad4_20_schedules.sql) and\n// the Workflows page edits it. A manual or webhook run always proceeds - a Run\n// button that obeyed the cadence would be a Run button that does nothing.\nconst d = $('Check schedule').first().json ?? {};\nconst decision = Array.isArray(d) ? d[0] : d;\n\n// A PERMISSION ERROR IS NOT AN UNAVAILABLE GATE. 42501 here means the key\n// cannot even call should_run, and a key that cannot do that cannot write\n// either - so stop, rather than failing open into a run that writes nothing.\n//\n// THIS NODE CANNOT CHECK THE KEY ITSELF ANY MORE - it is in the credential,\n// which an expression cannot read. So a refusal here is one of two things and\n// both stop the run: the credential holds the anon/publishable key rather than\n// the service role key, or service_role has not been granted what it needs.\n// Either way every write below would be refused the same way, so there is\n// nothing to gain by going on. ad4_38's output tells you which it was.\n//\n// ad4_38 revokes should_run from anon for this reason: it was granted so the\n// Workflows page could preview a cadence, that page reads v_execution_budget\n// instead, and the grant only let a bad key through the gate.\nif (decision && (String(decision.code) === '42501' ||\n                 /permission denied/i.test(String(decision.message || '')))) {\n  throw new Error(\n    `Supabase refused should_run: ${decision.message} (${decision.code}). Every write below ` +\n    `would be refused the same way, so nothing was fetched and nothing was written. Two ` +\n    `things cause this and the workflow cannot tell them apart, because the key now lives ` +\n    `in the n8n credential where an expression cannot read it. (1) THE KEY: open n8n -> ` +\n    `Credentials -> the Supabase account these nodes use and check Service Role Secret ` +\n    `holds the SERVICE ROLE key (Supabase -> Project Settings -> API -> service_role, or a ` +\n    `key beginning sb_secret_) - an anon key gets exactly this error, because anon is ` +\n    `granted SELECT and nothing else. NEVER put the service key anywhere starting ` +\n    `NEXT_PUBLIC_. (2) THE DATABASE: service_role has not been granted what it needs. Run ` +\n    `sql/ad4_38_grants.sql in the Supabase SQL editor - it grants the write path back and ` +\n    `then prints one row per ingest table saying whether the service key can write it, ` +\n    `which also tells you which of the two it was.`);\n}\n\n// A gate that fails closed would silently disable every workflow the first\n// time the settings row is missing or the RPC is absent. Fail OPEN: an\n// unreachable gate means run, and say so in the log.\nif (decision === null || decision === undefined || decision.run === undefined) {\n  console.log('schedule gate unavailable - running anyway');\n  return $input.all();\n}\n\nif (decision.run === false) {\n  // Recorded, so a skipped run is visible on the Workflows page as a skip\n  // rather than looking like a workflow that silently stopped working.\n  return [{ json: { __skipped: true, reason: decision.reason || 'not scheduled',\n                    minutes_since: decision.minutes_since ?? null,\n                    every_minutes: decision.every_minutes ?? null } }];\n}\nreturn $input.all();"
      }
    },
    {
      "id": "sched-stop",
      "name": "Stop if skipped",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        810,
        -30
      ],
      "parameters": {
        "jsCode": "// Returning [] stops the workflow here. Everything below this node\n// is skipped, which is the entire point of the gate.\nconst items = $input.all();\nif (items.length === 1 && items[0].json && items[0].json.__skipped) {\n  console.log('skipped: ' + items[0].json.reason);\n  return [];\n}\nreturn items;"
      }
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook Trigger": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Check schedule",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load cities": {
      "main": [
        [
          {
            "node": "Build requests",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build requests": {
      "main": [
        [
          {
            "node": "Fetch gridpoint",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch gridpoint": {
      "main": [
        [
          {
            "node": "Build rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build rows": {
      "main": [
        [
          {
            "node": "Guard: did anything parse?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Guard: did anything parse?": {
      "main": [
        [
          {
            "node": "Write forecast features",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write forecast features": {
      "main": [
        [
          {
            "node": "Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Summary": {
      "main": [
        [
          {
            "node": "Log run",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Log run": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check schedule": {
      "main": [
        [
          {
            "node": "Run now?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run now?": {
      "main": [
        [
          {
            "node": "Stop if skipped",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Stop if skipped": {
      "main": [
        [
          {
            "node": "Load cities",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
