# Two Vercel environment variables. Zero serverless functions (Revision A §9).
# The anon key is safe to expose - it is designed for browser use, and every
# table it can read is behind an explicit RLS policy (sql/ad4_rls.sql).
# NEVER put the service_role key here or in any NEXT_PUBLIC_ variable.
NEXT_PUBLIC_SUPABASE_URL=https://jittmxhzgqpifitwupss.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace_with_real_anon_key
